export interface City { // TODO - this model needs filtering (needs removing unused properties).
  countryCode: string,
  fcl: string,
  fcode: string,
  geonameId: string,
  lat: string,
  lng: string,
  name: string,
  toponymName: string
}